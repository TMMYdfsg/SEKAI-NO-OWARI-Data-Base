export interface Goods {
    id: string;
    name: string;
    category: string;
    price?: number;
    year?: number;
    description?: string;
    image?: string;
}

export const goods: Goods[] = [
    // Placeholder data
];
